package by.ladyka.poputka.data.enums;

/** Allowed query values for {@code GET /api/trip/owned}: {@code all}, {@code upcoming}, {@code past}. */
public enum OwnedTripTimeFilter {

    ALL,
    UPCOMING,
    PAST
}
